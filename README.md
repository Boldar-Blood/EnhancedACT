# EnhancedACT

This workspace contains the EnhancedACT project. It's a minimal scaffold created to get started.

Quick start

- `src/` - source code
- `tests/` - unit tests
- `docs/` - documentation

To run tests (if Python and pytest are installed):

```powershell
python -m pytest -q
```
